apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: exo-10-namespace
spec:
  selector:
    app: auth
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
