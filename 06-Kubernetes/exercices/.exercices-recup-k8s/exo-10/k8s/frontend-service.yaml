apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: exo-10-namespace
spec:
  selector:
    app: frontend
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
