apiVersion: v1
kind: Service
metadata:
  name: nom_service
spec:
  type: NodePort
  selector:
    key_a: value_a
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30001 # Valeur entre 30000 et 32767
