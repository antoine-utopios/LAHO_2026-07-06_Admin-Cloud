workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
    - if: $CI_COMMIT_TAG

default:
  tags:
    - docker
    - local

job-a:
  tags:
    - docker
    - local
  script:
    - echo "Etape A"
    - echo "Etape B"
    - echo "Etape C"
  rules:
    - if: $CI_COMMIT_REF_NAME != $CI_DEFAULT_BRANCH
job-b:
  tags:
    - gitlab-org
  script:
    - echo "Etape A"
    - echo "Etape B"
    - echo "Etape C"
  rules:
    - if: $CI_COMMIT_REF_NAME != $CI_DEFAULT_BRANCH
job-c:
  script:
    - echo "Etape A"
    - echo "Etape B"
    - echo "Etape C"
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH